# Local MiniStack contracts

This stack creates the SQS contracts in MiniStack for isolated service
development.

It is meant to be run by each service compose through the `terraform-contracts`
one-shot container.
